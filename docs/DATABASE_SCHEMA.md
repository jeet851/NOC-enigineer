# Database Schema

This document details the relational database schema of the NOC Copilot system.

```mermaid
erDiagram
    users {
        int id PK
        string username UK
        string password_hash
        string role
        string name
        string totp_secret
        boolean mfa_enabled
        int role_id FK
        string email
        boolean is_active
        datetime last_login
        datetime created_at
        datetime updated_at
    }
    roles {
        int id PK
        string name UK
        string description
    }
    permissions {
        int id PK
        string name UK
        string description
    }
    role_permissions {
        int role_id FK
        int permission_id FK
    }
    devices {
        string name PK
        string ip
        string vendor
        string platform
        string status
        string role
        string site
        string description
    }
    interfaces {
        int id PK
        string device_name FK
        string name
        string ip_address
        string mac_address
        string status
        string speed
        string description
    }
    telemetry_logs {
        int id PK
        datetime timestamp
        string device_name FK
        float ping_rtt
        float min_rtt
        float max_rtt
        float packet_loss
        float jitter
        int cpu
        int ram
        int interface_errors
        string status
        float temperature
        float disk_utilization
        float bw_in
        float bw_out
        int vpn_tunnels_up
        string bgp_peer_status
        int ospf_neighbor_count
        int fw_sessions
    }
    incidents {
        string id PK
        datetime timestamp
        string severity
        string device_name
        string site
        string vendor
        string description
        string business_impact
        string confidence
        string root_cause
        string status
        string evidence
        string remediation_commands
        string verification_steps
        string rollback_plan
        string risk_level
        string repair_time
        string engineering_report
        datetime resolved_at
        datetime updated_at
        string assigned_to
        string resolved_by
    }
    incident_timelines {
        int id PK
        string incident_id FK
        string status_from
        string status_to
        string changed_by
        datetime changed_at
        string notes
    }
    automation_jobs {
        int id PK
        string job_id UK
        string job_type
        string device_name
        string status
        datetime started_at
        datetime completed_at
        text output_log
        string triggered_by
    }
    slack_messages {
        int id PK
        string channel_id
        string user_id
        text message
        text response
        string persona
        datetime timestamp
    }
    compliance_reports {
        int id PK
        string report_type
        string incident_id FK
        text content
        string generated_by
        datetime generated_at
    }
    background_tasks {
        int id PK
        string task_id UK
        string task_name
        string status
        float progress_pct
        datetime started_at
        datetime finished_at
        text error
    }

    users ||--o| roles : "has role"
    roles ||--o{ role_permissions : "contains"
    permissions ||--o{ role_permissions : "applies to"
    devices ||--o{ interfaces : "has"
    devices ||--o{ telemetry_logs : "emits"
    incidents ||--o{ incident_timelines : "has lifecycle transitions"
    incidents ||--o{ compliance_reports : "documents"
```

## Description of New Tables

### 1. `incident_timelines`
Tracks transitions between active/assigned/resolved states for auditing resolution metrics and incident SLA parameters.

### 2. `automation_jobs`
Logs automation runs initiated via Ansible, Netmiko, or NAPALM playbooks with output streams and execution duration logs.

### 3. `slack_messages`
Persists dialog details for the Slack Bot interactions for compliance reporting and training corpus extraction.

### 4. `compliance_reports`
Stores standard operational procedure drafts (SOPs), runbooks (MOPs), and root-cause reports (RCAs) generated by the AI engine.

### 5. `background_tasks`
Monitors system scans and long-running sub-tasks triggered dynamically by developers or NOC engineers.
